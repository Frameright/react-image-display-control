# Otherwise prettier will fail in CI on Windows because of line endings get
# converted to CRLF when checking out the code.
# See https://prettier.io/docs/en/options.html#end-of-line
* text=auto eol=lf
